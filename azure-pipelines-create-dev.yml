# Create dev environment
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  'Snowflake_pool'

steps:

- task: CmdLine@2
  inputs:
    script: 'ls $(agent.builddirectory)/s'

# Create new database shell for development 
- task: CmdLine@2
  inputs:
    script: 'snowsql -a va_demo32.us-east-1 -u devops_user --filename $(agent.builddirectory)/s/Create_Dev_database.sql'
    workingDirectory: '/home/rshetler/bin'

